<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main2"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.hp.yourface.Main2Activity"
    android:background="@color/colorPrimary">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@android:color/background_light"
        android:id="@+id/imageView2"
        android:minHeight="350dp"
        android:minWidth="400dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="Save face"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/save"
        android:minWidth="400dp"
        android:minHeight="40dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:background="@color/colorPrimaryDark" />

    <Button
        android:text="Search for similar in the gallery"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/searchgallery"
        android:minWidth="400dp"
        android:minHeight="40dp"
        android:layout_above="@+id/save"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="14dp"
        android:background="@color/colorPrimaryDark" />

    <Button
        android:text="Search for similar in the internet"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/searchinternet"
        android:minWidth="400dip"
        android:minHeight="40dp"
        android:layout_marginBottom="14dp"
        android:layout_above="@+id/searchgallery"
        android:layout_alignParentStart="true"
        android:background="@color/colorPrimaryDark" />

</RelativeLayout>
